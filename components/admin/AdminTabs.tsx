/**
 * AdminTabs (stub)
 * Temporary no-op component to satisfy admin page import during the rescue build.
 * Replace with the real tab UI when the Admin area is re-enabled.
 */
export default function AdminTabs() {
  return null;
}

